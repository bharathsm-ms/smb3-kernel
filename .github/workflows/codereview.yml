name: Apply Mailing List Patch

on:
  workflow_dispatch:
    inputs:
      message_id:
        description: 'Patch message ID (e.g. <1234@linux-cifs>)'
        required: true
      author_name:
        description: 'Commit author name (your name)'
        required: true
      author_email:
        description: 'Commit author email (your GitHub email)'
        required: true

jobs:
  apply-patch:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.PERSONAL_GITHUB_TOKEN }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 10

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl git perl

      - name: Sanitize message ID
        id: sanitize
        run: |
          MSGID="${{ github.event.inputs.message_id }}"
          BRANCH=$(echo "$MSGID" | tr -d '<>' | tr '@.' '-' | tr -cd '[:alnum:]-')
          echo "branch_name=github-reviews-${BRANCH}" >> $GITHUB_OUTPUT

      - name: Download patch and create branch
        run: |
          curl -fsSL "https://lore.kernel.org/linux-cifs/${{ github.event.inputs.message_id }}/raw" -o patch.diff
          git checkout -b ${{ steps.sanitize.outputs.branch_name }}

      - name: Configure Git user
        run: |
          git config user.name "${{ github.event.inputs.author_name }}"
          git config user.email "${{ github.event.inputs.author_email }}"

      - name: Apply patch
        run: |
          if git apply --3way --verbose patch.diff; then
            git add .
            git commit -m "Applied patch from message ID: ${{ github.event.inputs.message_id }}"
          else
            echo "Patch failed to apply."
            exit 1
          fi

      - name: Run checkpatch.pl
        id: run_checkpatch
        run: |
          mkdir -p /tmp/checkpatch
          curl -fsSL https://raw.githubusercontent.com/torvalds/linux/master/scripts/checkpatch.pl -o /tmp/checkpatch/checkpatch.pl
          chmod +x /tmp/checkpatch/checkpatch.pl
          /tmp/checkpatch/checkpatch.pl --no-tree --patch patch.diff > /tmp/checkpatch/report.txt || true
          {
          echo "checkpatch<<EOF"
          cat /tmp/checkpatch/report.txt
          echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Push branch
        run: |
          git push https://x-access-token:${{ secrets.PERSONAL_GITHUB_TOKEN }}@github.com/${{ github.repository }} ${{ steps.sanitize.outputs.branch_name }}
          
      - name: Wait for GitHub to register branch
        run: sleep 5
        
      - name: Create Pull Request
        id: create_pr
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN }}
          title: "Apply patch ${{ github.event.inputs.message_id }}"
          body: |
            Patch from message ID: `${{ github.event.inputs.message_id }}`

            ## Checkpatch Report
            <details><summary>Click to view checkpatch.pl output</summary>

            ```
            ${{ steps.run_checkpatch.outputs.checkpatch }}
            ```

            </details>
          base: for-next
          head: ${{ steps.sanitize.outputs.branch_name }}
          author: ${{ github.event.inputs.author_name }} <${{ github.event.inputs.author_email }}>

      - name: Request Copilot Review
        run: |
          PR_URL="${{ steps.create_pr.outputs.pull-request-url }}"
          PR_NUMBER=$(basename "$PR_URL")
          gh pr edit "$PR_NUMBER" --add-reviewer github-copilot
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_GITHUB_TOKEN }}
